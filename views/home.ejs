<% if (user) { %>
	<% if (user.lang == 'en') { %>
		<% include en/partials/header %>
	<% } else if (user.lang == 'ko') { %>
		<% include ko/partials/header %>
	<% } else { %>
		<% include en/partials/header %>
   	<% } %>
<% } else { %>
	<% include en/partials/header %>
<% } %>

<link rel="stylesheet" href="/stylesheets/home.css">
<div class="content">
	
	<% if (user) { %>
		<% if (user.lang == 'en') { %>
			<a href="en/contents" class="btn btn-default btn-lg" >
	    		<img src="/icons/moa-picture.png" width="50px" height="50px"/>
	    		&nbsp; Share videos and pictures you loved watching!
	    	</a>
		<% } else if (user.lang == 'ko') { %>
			<a href="ko/contents" class="btn btn-default btn-lg" >
	    		<img src="/icons/moa-picture.png" width="50px" height="50px"/>
	    		&nbsp; 나누고 싶은 영상과 사진들을 올려주세요!
	    	</a>
	    <% } %>	   	
	<% } else if (moment.locale() == 'en'){ %>
		<a href="en/contents" class="btn btn-default btn-lg" >
			<img src="/icons/moa-picture.png" width="50px" height="50px"/>
    		&nbsp; Share videos and pictures you loved watching!
    	</a>
   	<% } else if (moment.local() == 'ko'){ %>
			<a href="ko/contents" class="btn btn-default btn-lg" >
    		<img src="/icons/moa-picture.png" width="50px" height="50px"/>
    		&nbsp; 나누고 싶은 영상과 사진들을 올려주세요!
    	</a>
   	<% }  else { %>
   		<a href="en/contents" class="btn btn-default btn-lg" >
			<img src="/icons/moa-picture.png" width="50px" height="50px"/>
    		&nbsp; Share videos and pictures you loved watching!
    	</a>
   	<% } %>
	
</div>

<% if (user) { %>
	<% if (user.lang == 'en') { %>
		<% include en/partials/footer %>
	<% } else if (user.lang == 'ko') { %>
		<% include ko/partials/footer %>
	<% } else { %>
		<% include en/partials/footer %>
   	<% } %>
<% } else { %>
	<% include en/partials/footer %>
<% } %>
